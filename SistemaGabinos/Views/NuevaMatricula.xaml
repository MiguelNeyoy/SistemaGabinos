<Page x:Class="SistemaGabinos.Views.NuevaMatricula"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Nueva Matrícula"
      Background="{StaticResource BrushSurfaceAlt}"
      FontFamily="{StaticResource FontPoppins}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" MaxWidth="560">

            <!-- Page Title -->
            <TextBlock Text="Nueva Matrícula"
                       Style="{StaticResource HeadlineLarge}"
                       Margin="0,0,0,24" />

            <!-- Form Card -->
            <Border Style="{StaticResource CardStyle}" Padding="24">
                <StackPanel>

                    <!-- CURP -->
                    <TextBlock Text="CURP" Style="{StaticResource LabelMedium}" Margin="0,0,0,4" />
                    <TextBox Style="{StaticResource InputStyle}"
                             Text="{Binding Curp, UpdateSourceTrigger=PropertyChanged}"
                             MaxLength="18" />

                    <!-- Nombre Completo -->
                    <TextBlock Text="Nombre Completo" Style="{StaticResource LabelMedium}" Margin="0,16,0,4" />
                    <TextBox Style="{StaticResource InputStyle}"
                             Text="{Binding NombreCompleto, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Fecha de Nacimiento -->
                    <TextBlock Text="Fecha de Nacimiento" Style="{StaticResource LabelMedium}" Margin="0,16,0,4" />
                    <DatePicker SelectedDate="{Binding FechaNacimiento}"
                                FontFamily="{StaticResource FontPoppins}"
                                FontSize="14" />

                    <!-- Teléfono -->
                    <TextBlock Text="Teléfono" Style="{StaticResource LabelMedium}" Margin="0,16,0,4" />
                    <TextBox Style="{StaticResource InputStyle}"
                             Text="{Binding Telefono, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Curso -->
                    <TextBlock Text="Curso" Style="{StaticResource LabelMedium}" Margin="0,16,0,4" />
                    <ComboBox ItemsSource="{Binding Cursos}"
                              SelectedItem="{Binding CursoSeleccionado}"
                              DisplayMemberPath="Nombre"
                              FontFamily="{StaticResource FontPoppins}"
                              FontSize="14"
                              Padding="12,8" />

                    <!-- Tutor Section Divider -->
                    <Border BorderBrush="{StaticResource BrushBorder}"
                            BorderThickness="0,1,0,0"
                            Margin="0,20,0,16"
                            Padding="0,12"
                            Visibility="{Binding VisibleTutor}">
                        <TextBlock Text="Datos del Tutor (obligatorio si es menor de 18 años)"
                                   Style="{StaticResource BodyMedium}"
                                   FontStyle="Italic"
                                   Foreground="{StaticResource BrushTextMuted}" />
                    </Border>

                    <!-- Nombre del Tutor -->
                    <TextBlock Text="Nombre del Tutor" Style="{StaticResource LabelMedium}" Margin="0,0,0,4" />
                    <TextBox Style="{StaticResource InputStyle}"
                             Text="{Binding NombreTutor, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Parentesco -->
                    <TextBlock Text="Parentesco" Style="{StaticResource LabelMedium}" Margin="0,16,0,4" />
                    <TextBox Style="{StaticResource InputStyle}"
                             Text="{Binding ParentescoTutor, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Teléfono del Tutor -->
                    <TextBlock Text="Teléfono del Tutor" Style="{StaticResource LabelMedium}" Margin="0,16,0,4" />
                    <TextBox Style="{StaticResource InputStyle}"
                             Text="{Binding TelefonoTutor, UpdateSourceTrigger=PropertyChanged}" />

                </StackPanel>
            </Border>

            <!-- Cobro Inicial Card -->
            <!--
                Contratos de Binding (ViewModel debe exponer):
                  ConceptosCobro (ObservableCollection<ConceptoCobroItem>) → ListBox.ItemsSource
                    - ConceptoCobroItem.Seleccionado (bool)  → CheckBox.IsChecked
                    - ConceptoCobroItem.NombreVisual (string) → TextBlock.Text
                    - ConceptoCobroItem.Monto (decimal)       → TextBox.Text
                    - ConceptoCobroItem.MontoEsEditable (bool) → TextBox.IsReadOnly (invertido via InvertBoolConverter)
                  Total (decimal)          → TextBlock.Text
                  MetodoPagoSeleccionado (MetodoPago?) → ComboBox.SelectedItem
                  MetodosPago (ObservableCollection<MetodoPago>) → ComboBox.ItemsSource
                  CobroVisible (Visibility) → Card.Visibility
                  TicketVisible (Visibility) → Button.Visibility
                  MensajeExitoVisible (Visibility) → StackPanel.Visibility
                  Mensaje (string) → TextBlock.Text
                  MensajeError (string) → TextBlock.Text
                  MensajeErrorVisible (Visibility) → TextBlock.Visibility
                  RegistrarCommand (IRelayCommand) → Button.Command
                  GenerarTicketCommand (IRelayCommand) → Button.Command
            -->
            <Border Style="{StaticResource CardStyle}" Padding="24"
                    Margin="0,20,0,0"
                    Visibility="{Binding CobroVisible}">
                <StackPanel>
                    <TextBlock Text="Cobro Inicial"
                               Style="{StaticResource HeadlineMedium}"
                               Margin="0,0,0,16" />

                    <!-- Checklist de conceptos -->
                    <Border BorderBrush="{StaticResource BrushBorder}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="0">
                        <ListBox ItemsSource="{Binding ConceptosCobro}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="12,8"
                                            BorderBrush="{StaticResource BrushBorder}"
                                            BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="100" />
                                            </Grid.ColumnDefinitions>

                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding Seleccionado}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0" />

                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding NombreVisual}"
                                                       Style="{StaticResource BodyMedium}"
                                                       VerticalAlignment="Center" />

                                            <TextBox Grid.Column="2"
                                                     Text="{Binding Monto, StringFormat=N2}"
                                                     Style="{StaticResource InputStyle}"
                                                     IsReadOnly="{Binding MontoEsEditable, Converter={StaticResource InvertBoolConverter}}"
                                                     TextAlignment="Right"
                                                     Padding="8,4"
                                                     FontSize="14" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>

                    <!-- Total -->
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="Total"
                                   Style="{StaticResource TitleLarge}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding Total, StringFormat={}${0:N2}}"
                                   Style="{StaticResource TitleLarge}"
                                   Foreground="{StaticResource BrushPrimary}"
                                   TextAlignment="Right"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <!-- Método de Pago -->
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="Método de Pago"
                                   Style="{StaticResource LabelMedium}"
                                   VerticalAlignment="Center" />
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding MetodosPago}"
                                  SelectedItem="{Binding MetodoPagoSeleccionado}"
                                  FontFamily="{StaticResource FontPoppins}"
                                  FontSize="14"
                                  Padding="12,8" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Submit Button -->
            <Button Content="Registrar y Cobrar"
                    Style="{StaticResource ButtonPrimary}"
                    Command="{Binding RegistrarCommand}"
                    Margin="0,20,0,0"
                    HorizontalAlignment="Left"
                    Width="200" />

            <!-- Success Message -->
            <StackPanel Margin="0,12,0,0"
                        Visibility="{Binding MensajeExitoVisible}">
                <TextBlock Text="{Binding Mensaje}"
                           Foreground="{StaticResource BrushPrimary}"
                           Style="{StaticResource BodyMedium}"
                           TextWrapping="Wrap" />
                <Button Content="Generar Ticket"
                        Style="{StaticResource ButtonAccent}"
                        Command="{Binding GenerarTicketCommand}"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Left"
                        Width="180"
                        Visibility="{Binding TicketVisible}" />
            </StackPanel>

            <!-- Error Message -->
            <TextBlock Text="{Binding MensajeError}"
                       Foreground="{StaticResource BrushError}"
                       Margin="0,12,0,0"
                       Style="{StaticResource BodyMedium}"
                       TextWrapping="Wrap"
                       Visibility="{Binding MensajeErrorVisible}" />

        </StackPanel>
    </ScrollViewer>
</Page>