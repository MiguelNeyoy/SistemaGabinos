<Page x:Class="SistemaGabinos.Views.NuevaMatricula"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:SistemaGabinos.Controls"
      Title="Nueva Matrícula"
      Background="{StaticResource BrushSurfaceAlt}"
      FontFamily="{StaticResource FontPoppins}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">

            <TextBlock Text="Nueva Inscripción"
                       Style="{StaticResource HeadlineLarge}"
                       Margin="0,0,0,4" />
            <TextBlock Text="Completa los campos para registrar a un nuevo alumno"
                       Style="{StaticResource BodyMedium}"
                       Foreground="{StaticResource BrushTextMuted}"
                       Margin="0,0,0,24" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <controls:CardSection Grid.Column="0" Title="Datos del Alumno">
                    <StackPanel>
                        <controls:IconInput Label="CURP (Obligatorio)"
                                            Text="{Binding Curp}" MaxLength="18" />
                        <controls:FormField Label="Nombre Completo"
                                            Text="{Binding NombreCompleto}"
                                            Margin="0,16,0,0" />
                        <controls:IconInput Label="Teléfono de Contacto" 
                                            Text="{Binding Telefono}"
                                            Margin="0,16,0,0" />

                        <TextBlock Text="Fecha de Nacimiento"
                                   Style="{StaticResource LabelMedium}"
                                   Margin="0,16,0,4" />
                        <DatePicker SelectedDate="{Binding FechaNacimiento}"
                                    FontFamily="{StaticResource FontPoppins}"
                                    FontSize="14" />

                        <controls:RadioList Label="Libro"
                                            ItemsSource="{Binding Cursos}"
                                            SelectedItem="{Binding CursoSeleccionado}"
                                            Margin="0,16,0,0" />

                        <Border BorderBrush="{StaticResource BrushBorder}"
                                BorderThickness="0,1,0,0"
                                Margin="0,20,0,16"
                                Padding="0,12"
                                Visibility="{Binding VisibleTutor}">
                            <TextBlock Text="Datos del Tutor (obligatorio si es menor de 18 años)"
                                       Style="{StaticResource BodyMedium}"
                                       FontStyle="Italic"
                                       Foreground="{StaticResource BrushTextMuted}" />
                        </Border>

                        <controls:FormField Label="Nombre del Tutor"
                                            Text="{Binding NombreTutor}" />
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <controls:FormField Grid.Column="0" Label="Parentesco"
                                                Text="{Binding ParentescoTutor}" />
                            <controls:FormField Grid.Column="2" Label="Teléfono del Tutor"
                                                Text="{Binding TelefonoTutor}" />
                        </Grid>
                    </StackPanel>
                </controls:CardSection>

                <controls:CardSection Grid.Column="2" Title="Caja / Pago Inicial"
                                      Visibility="{Binding CobroVisible}">
                    <StackPanel>
                        <controls:ChecklistBox ItemsSource="{Binding ConceptosCobro}"
                                                Total="{Binding Total}" />
                        <Border BorderBrush="{StaticResource BrushBorder}"
                                BorderThickness="0,1,0,0"
                                Margin="0,12,0,12" />
                        <controls:ToggleGroup Label="Método de Pago"
                                              Option1Text="Efectivo"
                                              Option1Checked="{Binding EfectivoSeleccionado}"
                                              Option2Text="Transferencia"
                                              Option2Checked="{Binding TransferenciaSeleccionado}" />
                        <Border BorderBrush="{StaticResource BrushBorder}"
                                BorderThickness="0,1,0,0"
                                Margin="0,12,0,12" />
                        <Button Content="Generar Ticket"
                                Style="{StaticResource ButtonSecondary}"
                                Command="{Binding GenerarTicketCommand}"
                                Height="45"
                                Visibility="{Binding TicketVisible}"
                                HorizontalAlignment="Left" />
                    </StackPanel>
                </controls:CardSection>
            </Grid>

            <Button Content="Guardar Matrícula"
                    Style="{StaticResource ButtonPrimary}"
                    Command="{Binding RegistrarCommand}"
                    Margin="0,24,0,0"
                    HorizontalAlignment="Center"
                    Width="240" />

            <TextBlock Text="Al guardar, el alumno quedará matriculado en el sistema."
                       Style="{StaticResource BodyMedium}"
                       Foreground="{StaticResource BrushTextMuted}"
                       TextAlignment="Center"
                       Margin="0,12,0,0" />

            <StackPanel Margin="0,12,0,0"
                        Visibility="{Binding MensajeExitoVisible}">
                <TextBlock Text="{Binding Mensaje}"
                           Foreground="{StaticResource BrushPrimary}"
                           Style="{StaticResource BodyMedium}"
                           TextWrapping="Wrap"
                           TextAlignment="Center" />
            </StackPanel>

            <TextBlock Text="{Binding MensajeError}"
                       Foreground="{StaticResource BrushError}"
                       Margin="0,12,0,0"
                       Style="{StaticResource BodyMedium}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Visibility="{Binding MensajeErrorVisible}" />

        </StackPanel>
    </ScrollViewer>
</Page>
